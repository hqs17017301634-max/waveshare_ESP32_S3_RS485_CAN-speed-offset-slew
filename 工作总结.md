# 工作总结 / WORK SUMMARY

> 规则：每次开工**先读本文件**再动手；每次完成改动**回来更新本文件**。
> 用途：跨会话保留上下文，减少压缩损失。

最后更新：2026-06-03

---

## 0. 两个项目 / 分支（别搞混）

| 项目 | 路径 | 框架 | 当前分支 | 内容 |
|---|---|---|---|---|
| **RP2040**（当前主战场） | `C:\Users\Administrator\Desktop\FSD-CAN\AAA-ESP32S3\RP2040` | Arduino / PlatformIO | `T-2CAN-WEB` | 双 CAN + 轻量 WebUI + 灯光爆闪 + 录制 |
| **T-2CAN**（ESP-IDF dashboard） | `C:\Users\Administrator\Desktop\FSD-CAN\AAA-ESP32S3\T-2CAN` | ESP-IDF / PlatformIO | `main` | 完整 dashboard，已做高光爆闪/0x249 |

- `T-2CAN-WEB` 分支**只在 RP2040 项目里**；T-2CAN 项目没有这个分支。
- 提交由**用户自己管理**，AI 改完默认**不自动提交**，除非用户说“提交”。
- 不要提交：`.pio/`、`Downloads/`、`opendbc-master/`、`源代码与抓包数据/`。

## 1. 关键编译命令（RP2040）

```powershell
cd C:\Users\Administrator\Desktop\FSD-CAN\AAA-ESP32S3\RP2040
pio run -e lilygo_t2can_arduino_webui      # 带 WebUI（灯光/预热功能都在这）
pio run -e lilygo_t2can_arduino            # 不带 WebUI
pio run -e lilygo_t2can_arduino_webui -t upload --upload-port COM23
```
WebUI 热点：SSID `T2CAN-FSD` / 密码 `12345678` / **`http://100.100.1.1`**（softAPConfig 设定，2026-06-03 起）

## 2. 主要文件（RP2040）

- `ESP32S3CAN-FSD/ESP32S3CAN-FSD.ino` — 主固件（CAN A FSD + CAN B 灯光 + WebUI + 录制）
- `ESP32S3CAN-FSD/web_ui_page.h` — WebUI 页面（**手写**，不经 minify）
- `README.md` — 中英文文档

## 3. ⚠️ 已知坑

- **`.ino` 是 UTF-8，但中文注释是 GBK 乱码（mojibake）**：被非 UTF-8 编辑器存过。
  能正常编译。AI 改这个文件时：**只匹配 ASCII 行**，新注释**写英文**，别加中文，避免加重乱码。
  （`web_ui_page.h`、`README.md` 是干净 UTF-8，可正常写中文。）
- 待办：可单独把 `.ino` 转回干净 UTF-8（用户未要求时先不动）。

## 4. CAN B 灯光/拨杆 关键报文与字段（实车抓包结论）

- **0x249 SCCMLeftStalk**：`byte1` 低 4 位=counter(0-15 滚动)，bits4-5=status；
  status=1=PULL(超车灯/Flash-to-pass)，0=idle。`byte0 = BASE[counter] XOR OFFSET[status]`（已破解）。
  → 用于**高光爆闪**（双拉触发，8 次）。
- **0x3C2 VCLEFT_switchStatus（多路复用！）**：`byte0 bits0..1`=mux 索引。
  - **mux0**(`b0&3==0`)：承载 `byte0 bit3`=hazardButtonPressed(按下=0x08) + 计数器/CRC；此 mux 的 `data[3]=0x55` 是填充，**不是滚轮**。抓包按双闪 = `08 55 55 55 00 00 59 45`。
  - **mux1**(`b0&3==1`)：承载 `data[3]`=rightScrollTicks(6-bit 有符号，空闲 0，+前/-后)。空闲样例 `41 85 0 0 0 0 0 128`。
  - ⚠️ **坑（cap64 实测）**：不分 mux 直接读 data[3] 会把 mux0 的 0x55 误读成 +21 假滚轮，踩刹车换 R 时触发“D 意图→取消”，导致倒车双闪只闪 1~2 下就停。**已修**：滚轮只在 mux1 读；双闪注入只在 mux0（缓存最近 mux0 帧）。
  → 用于**双闪注入(mux0)** + **右滚轮+刹车 档位识别(mux1)**。
- **0x3F5 VCFRONT_lighting**：只读灯态反馈，**不注入**。
  （byte3 bit5 远光实点亮 / bit4 自动远光 / byte4 bit5 手动远光 / byte1 bit7 大灯手动模式）
- **0x273 body lighting**：后雾灯，复用最新实时帧只切 `data[2] bit7`（0x80）。
- **0x082 UI_tripPlanning**：电池预热。ON=`AF 50 AC 3C FF 03 9A 0F`，OFF=`01 50 AC 3C FF 03 9A 0F`，周期 1000ms。
- **0x109 SBW_RQ_SCCM**：换挡请求帧，**CRC/档位值未知，禁止伪造注入**（风险高）。需要再抓右拨杆 D/R 才能破解。

## 5. 各功能现状（RP2040 / T-2CAN-WEB）

| 功能 | 配置开关 | 状态 |
|---|---|---|
| 高光爆闪 | `highBeamStrobeEnabled` | ✅ 双拉 0x249 PULL 触发 8 次 75/75ms |
| 后雾灯刹车爆闪 | `rearFogBrakeStrobeEnabled` | ✅ 0x273，缓减速 3 次/急减速 6 次 |
| 倒挡双闪+后雾灯 | `reverseStrobeEnabled` | ✅ 触发=0x118 R 档 或 刹车+0x3C2 右滚轮向后；输出=0x3C2 hazard + 0x273 雾灯 4 次（**双闪按钮帧待实车验证**） |
| 电池预热 | `batteryPreheatEnabled` | ✅ 0x082 实车 ON/OFF payload，1000ms |
| CSV 录制 | — | ✅ `/rec_*`，默认 ID 含 082/249/273/3F5… |

## 6. 最近一次会话做了什么（2026-06-03）

**已提交并推送到 GitHub**：`57a71d6`（倒车爆闪/后雾灯/电池预热初版）+ `7ebe69e`（本次修正）。
origin = `github.com/hqs17017301634-max/waveshare_ESP32_S3_RS485_CAN-speed-offset-slew`，分支 `T-2CAN-WEB`。

本次（`7ebe69e`）依据实车抓包(cap64)修正：
1. **电池预热**：`0x082 data[0]=0x05`/500ms → 完整 ON `AF 50 AC 3C FF 03 9A 0F` / OFF `01 50 AC 3C FF 03 9A 0F` + 1000ms + 关闭补发 OFF 帧。
2. **双闪**：删 `stalkTurnFrame`(0x249 turn-field)，新 `vcleftHazardFrame()` 走 `0x3C2 byte0 bit3`，且**只在 mux0** 注入(缓存最近 mux0 帧)。
3. **右滚轮+刹车 档位识别**：`0x3C2` **只在 mux1** 读 `data[3]` 6-bit 有符号 + `g_brakePedalActive`(来自 0x145)；
   刹车+后滚(<0)→R 触发，刹车+前滚(>0)→D 取消；**不发 0x109**。`0x3C2` 已加入 CAN B RX 过滤(RXF5)。
4. **修复假滚轮 bug**：旧代码不分 mux 把 mux0 的 0x55 误读成 +21，换 R 时自我取消(只闪1~2下)。已按 mux 区分解决。
5. **倒车双闪节奏**：实车发现 0x3C2 是**瞬时 toggle 按钮**，脉冲 4 次=开/关/开/关(只看到 2 段亮)。
   改为**状态机**：按一下开(`REVERSE_HAZARD_CLICK_MS`=200ms) → 保持 `REVERSE_HAZARD_ON_MS`=2500ms(车连续闪) → 再按一下关。后续若仍不对，可能要按实车 hazard 反馈(0x3F5)判断当前状态。
6. **WebUI SoftAP IP**：`softAPConfig` 设为 **100.100.1.1**（页面 `http://100.100.1.1`）。
- 同步更新 `web_ui_page.h` 提示 + `README.md`（中英文，含 IP 与 0.5s 节奏）。
- 编译通过：`lilygo_t2can_arduino_webui` SUCCESS，Flash 23.8%；已烧 COM23。

## 7. 待办 / 下一步

- [x] `.gitignore` 加入 `Downloads/`、`opendbc-master/`、`源代码与抓包数据/`（解决 IDE “+7M 变更”假象）。
- [x] 本次改动已提交 `7ebe69e` 并推送 origin。
- [ ] **用滚轮真正挂挡（用户选定目标）**：必须先抓到 `0x109 SBW_RQ_SCCM`。
  cap64 两条总线都无 265：CAN B(accept-all,308 IDs) 没有 → 不在 X197 9/10；CAN A 被 TWAI 过滤(只 8 个 ID)看不到。
  **下一步**：做“CAN A 全量抓包”固件(放开 TWAI 过滤+录全部)，实车 D↔R 切几次抓 0x109，再破解档位值+CRC(可能是标准 `teslaCanChecksum`)。⚠️ 注入换挡命令安全攸关，需严格门控。
- [ ] **实车验证**：0x3C2 双闪按钮是瞬时切换语义；4 次按/松可能只显示 2 段亮，必要时改“按一下开→保持→再按一下关”。
- [ ] （可选）把 `.ino` 编码转回干净 UTF-8。
- [ ] T-2CAN(ESP-IDF) 项目的高光爆闪提交 2978f47 在 `main`，是否需要单独分支由用户定。
